import React from 'react';
import LoadData from './components/async/LoadData';

const BucketView : React.FC = props => {
    return (
        <LoadData promise={}>
            {
                
            }
        </LoadData>
    )
}
export default BucketView;